# ft_printf (모듈)

이 디렉터리는 `ft_printf`의 실제 구현 소스가 위치합니다. 상위 `ft_printf/README.md`를 참고하세요.

- 핵심 파일:
  - `ft_printf.c`: 포맷 루프와 가변인자 처리
  - `ft_printf_utils.c`: 진법/대소문자 설정과 숫자 출력
  - `ft_printf.h`: 상수/구조체/프로토타입
- 의존: 상위 `libft` 모듈